services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ohm
      POSTGRES_PASSWORD: ohm
      POSTGRES_DB: ohm_development
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
