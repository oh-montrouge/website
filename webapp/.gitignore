# Buffalo build artifacts
bin/
tmp/

# Environment files (see .env.*.example for templates)
.env
.env.*
!.env.*.example

# Generated assets (rebuilt from public/assets/ sources)
public/assets/application.js
public/assets/buffalo.css
# application.css is hand-authored (OHM design system) — tracked in git

# Node (not used, but guard against accidental installs)
node_modules/
